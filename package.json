{
	"name": "schoollab-rest-api",
	"version": "0.0.1",
	"dependencies": {
		"aws-sdk": "^2.312.0",
		"uuid": "^3.3.2"
	},
	"scripts": {
		"deploy-dev": "serverless deploy --stage dev --region us-east-1",
		"deploy-staging": "serverless deploy --stage staging --region us-east-1"
	}
}
